#include <stdio.h>


main() {
 double matrix[][];
 double startVector[];
 double resultVector[];
 int n, m, readMatrix, readVector;
 int *nPointer, *mPointer;
 
 nPointer = &n;
 mPointer = &m;

 int read_mat(int *, int *, double **)
 int read_vector(int , double **)
 readMatrix = read_mat(mPointer, nPointer, matrix);
 readVector = read_vector(m, startVector);

//INCOMPLETE
}
