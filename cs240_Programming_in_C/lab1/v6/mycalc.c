/*	function mycalc(a,b) takes two
	float arguments a and b, subtracts b from a
	and returns the result to the calling
	function */

float mycalc(float a, float b)
{
float c;

// subtract
  c = a - b;

  return c;
}
